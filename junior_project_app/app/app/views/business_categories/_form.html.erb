<%= form_for(@business_category) do |f| %>
  <% if @business_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@business_category.errors.count, "error") %> prohibited this business_category from being saved:</h2>

      <ul>
      <% @business_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :business_id %><br>
    <%= f.collection_select(:business_id, Business.all, :id, :name, prompt: true) %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.collection_select(:category_id, Category.all, :id, :name, prompt: true) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
